name : Primo GitHub action
on :
  push:
jobs : 
  italian-job:
    runs-on: ubuntu-latest
    steps: 
      - uses: action/checkout@v4
      - run: ls 
      - run: sudo apt update
      - run: pwd
      - run: ls -la
      - run: java -version
